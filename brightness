# Tone down brightness
# xrandr --output eDP-1 --brightness 1.0 # Max colour brightness, [0.0, 1.0]

# Tone down light intensity
# sudo vim /sys/class/backlight/intel_backlight/brightness 

# Max brightness level comes from: /sys/class/backlight/intel_backlight/max_brightness


# Get connected screen names 
# xrandr -q | grep "connected"
